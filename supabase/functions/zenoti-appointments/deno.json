{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --allow-read --watch index.ts",
    "deploy": "deployctl deploy --project=zenoti-appointments --import-map=../deno.json --no-static index.ts"
  }
}